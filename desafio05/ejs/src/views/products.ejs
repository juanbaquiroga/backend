<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        .main{
            background-color: rgb(206, 206, 206);
            width: 100%;
            height: 100vh;
            margin: 15px;
            display: flex;
            flex-flow: column nowrap;
            align-items: center;
        }
        .tableTitle{
            color: rgb(255, 255, 255);
        }
        tr{
            border:solid 1px wheat;
        }
        th, td{
            text-align: left;
        }
        .table{
            color: rgb(187, 187, 187);
            border: solid 1.5px  rgb(81, 88, 96);
            background-color: rgb(53, 58, 63); 
            padding: 10px;
            width: 50%;
        }
        .containerImg{
            width: 100px;
        }
    </style>
</head>
<body>
    <div class="main">
        <h1>Tienda de productos</h1>
        <a href="http://localhost:3000/">ir formulario de carga</a>
            <%if(hasAny) {%>
            <table class="table">
                <tr class="tableRow">
                    <th class="tableTitle">Nombre</th>
                    <th class="tableTitle">Precio</th>
                    <th class="tableTitle">Foto</th>
                </tr>
                <% products.forEach(product => {%>
                    <tr class="tableRow">
                    <td class="productTitle"><%=product.title%></td>
                    <td class="productPrice">$<%=product.price%></td>
                    <td class="containerImg"><img class="productImg" src='<%=product.img%>' alt='<%=product.title %>' width="100"></td>
                    </tr>
                <% }) %>
            </table>
            <% } else { %>
            <h1>No hay productos</h1>
            <% } %>
    </div>

</body>
</html>